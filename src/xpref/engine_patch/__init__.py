"""Bundled engine patches shipped with xpref.

``llama_cpp_router_logit.patch`` — a reference unified diff against the
llama.cpp kimi-k3 fork that exports router gating logits + fired expert ids
into the shared ring ``xpref attach`` reads.
"""
